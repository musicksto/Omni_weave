# OmniWeave — Cloud Build pipeline
# Builds ADK server container, deploys to Cloud Run, builds frontend, deploys to Firebase Hosting
# Usage: gcloud builds submit --config cloudbuild.yaml .

steps:
  # Step 1: Build the ADK server container
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - '${_REGION}-docker.pkg.dev/$PROJECT_ID/omniweave/${_SERVICE_NAME}:$COMMIT_SHA'
      - '-t'
      - '${_REGION}-docker.pkg.dev/$PROJECT_ID/omniweave/${_SERVICE_NAME}:latest'
      - './server'
    id: 'build-server'

  # Step 2: Push container to Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - '--all-tags'
      - '${_REGION}-docker.pkg.dev/$PROJECT_ID/omniweave/${_SERVICE_NAME}'
    id: 'push-server'
    waitFor: ['build-server']

  # Step 3: Deploy to Cloud Run with required environment variables
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'gcloud'
    args:
      - 'run'
      - 'deploy'
      - '${_SERVICE_NAME}'
      - '--image'
      - '${_REGION}-docker.pkg.dev/$PROJECT_ID/omniweave/${_SERVICE_NAME}:$COMMIT_SHA'
      - '--region'
      - '${_REGION}'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--port'
      - '8080'
      - '--memory'
      - '1Gi'
      - '--cpu'
      - '1'
      - '--set-env-vars'
      - 'GOOGLE_GENAI_USE_VERTEXAI=TRUE,GOOGLE_CLOUD_PROJECT=$PROJECT_ID,GOOGLE_CLOUD_LOCATION=${_LOCATION},GOOGLE_API_KEY=${_GOOGLE_API_KEY},GEMINI_API_KEY=${_GOOGLE_API_KEY},GOOGLE_GENAI_API_KEY=${_GOOGLE_API_KEY}'
    id: 'deploy-cloudrun'
    waitFor: ['push-server']

  # Step 4: Install frontend dependencies
  - name: 'node:22'
    entrypoint: 'npm'
    args: ['install']
    id: 'install-deps'

  # Step 5: Build frontend
  - name: 'node:22'
    entrypoint: 'npm'
    args: ['run', 'build']
    id: 'build-frontend'
    waitFor: ['install-deps']

  # Step 6: Deploy Firestore rules + Firebase Hosting
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        npm install -g firebase-tools
        firebase deploy --only firestore:rules,storage --project $PROJECT_ID
        firebase deploy --only hosting --project $PROJECT_ID
    id: 'deploy-hosting'
    waitFor: ['build-frontend']

substitutions:
  _REGION: 'us-central1'
  _LOCATION: 'global'
  _SERVICE_NAME: 'omniweave-adk'
  _GOOGLE_API_KEY: ''

options:
  logging: CLOUD_LOGGING_ONLY

images:
  - '${_REGION}-docker.pkg.dev/$PROJECT_ID/omniweave/${_SERVICE_NAME}:$COMMIT_SHA'
  - '${_REGION}-docker.pkg.dev/$PROJECT_ID/omniweave/${_SERVICE_NAME}:latest'
